[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "snakecase"
version = "1.0.1"
description = "Vendored replacement for the snakecase package (removed from PyPI), required by valentine."
requires-python = ">=3.8"
