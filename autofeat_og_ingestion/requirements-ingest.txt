# Dependency closure for ingestion only (ingest_nodes + profile_valentine_dataset).
# Versions match the environment in which the reference graph was built
# (Python 3.12.3). The full feature-discovery pipeline has more dependencies
# (see pyproject.toml), but they are not needed for ingestion.
neo4j==4.4.0
valentine==0.5.0
pandas==2.3.3
numpy==2.1.3
joblib==1.2.0
tqdm==4.67.1
# joblib 1.2.0 imports distutils, which Python 3.12 removed from the standard
# library; setuptools restores it via its compatibility shim.
setuptools==80.10.2
